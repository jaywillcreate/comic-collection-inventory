{
  "name": "longbox-archive-web",
  "version": "1.0.0",
  "description": "Longbox Archive frontend — public catalog + connected CMS, recreated from the Nocturne design handoff against the Longbox Archive API.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.7",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.5"
  }
}
